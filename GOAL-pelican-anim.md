# 骑自行车的鹈鹕 · 2D 循环动画

> **用法**：把「粘贴区」内的全文，原样粘进新建集群的「任务目标（Mission + DoD）」框。
> 建议一并填：显示名称 `PELICAN LOOP`、智能体数 `6`、预算 `$12`。
> 填了显示名称，集群 id 才会是干净的 `pelican-loop`，否则 id 会从正文派生出一串乱码。

---

## 粘贴区 ↓↓↓（以下全文粘贴，不含本行与上方说明）

【Swarm 目标】骑自行车的鹈鹕 —— 一段 2D 循环动画（单文件 SVG）

【这件事的来历】「画一只骑自行车的鹈鹕」是著名的单次生成基准。把它做成动画，是同一道题更难的一档：单模型是盲写的——它们永远看不见自己的动画，只能凭空想象时间在流动。而动画的谎言，只有在帧与帧连起来看时才会暴露：脚在踏板上打滑、轮子不转甚至倒转、部件互相穿模、循环的接缝处跳一下。你们不盲。你们可以把动画逐帧渲染出来、逐帧测量，摊成一张接触表（contact sheet）一眼看完整条时间线，然后作为一支队伍迭代。你们的任务：做出机器有史以来最好的、骑自行车鹈鹕的 2D 循环动画。

【交付物】在 `final_output/pelican-anim.svg` 生成**一个自包含的 SVG 文件**，内含一段 **2–4 秒无缝循环**的 2D 动画：一只正在骑自行车的鹈鹕。

硬性边界（违反即不合格）：
- 纯手写 SVG 标记 + 声明式动画（SMIL 的 `<animate>` / `<animateTransform>`，或内联 CSS animation）。
- 单文件：不得依赖任何外部素材、外链字体、CDN、外部库、栅格图或视频文件。
- 禁止逐帧贴图式的伪动画：不允许把每一帧画成一张静态图轮播。运动必须由时序 / 变换声明驱动。
- 在 Chromium 1280×720 视口打开即自动播放，无需点击、无需任何交互。

【协作契约 —— 这比分工本身更重要】
动画里有多处**运动学耦合**（如：脚心必须同时满足「绕髋圆」和「绕曲柄圆」两个圆方程），两端若分给两个 agent，极大概率对不上。为防这一点，必须按下列顺序产出契约，**任何后续切片都只能引用契约里的常量和坐标，不得自编**：

1. **先行切片「动画坐标系冻结」**：在动任何部件之前，先跑一片专门产出一份契约文件（建议 `workspace/animation_contract.json`），冻结以下常量：
   - 前 / 后车轮圆心 (cx, cy) 与半径 r
   - 曲柄圆心 (cx, cy)、曲柄半径、踩踏周期 T
   - 髋关节圆心 (cx, cy)、腿长 L
   - 所有以上坐标的**绝对画面像素值**（必须与 SVG 最终位置一致）
   - 相位约定（t=0 时曲柄指向、腿 1 相位）
2. **所有运动切片开工前先读契约**：每个做运动的 agent 在动手前必须 `read` 契约文件，把它当常量表；自己的代码里出现的坐标/半径/角度**必须来自契约**，不得凭空猜。
3. **契约一旦写进文件就不许二次修改**：后续若发现某常量不对，要么换契约（走一次完整重写并重新冻结），要么通过 `send_mail` 拉回原切片负责人协商；不得偷偷改自己的局部参数去凑。
4. 切片不预先分配，但**契约切片应优先认领**（它是后续所有运动的依赖前驱）。

**这条契约不是装饰**：上一轮同样的任务，腿与踏板分属两个切片，各自手写圆心和半径，导致脚每圈只在 t≈0.15s 那一瞬碰到踏板、其余时间距踏板 200–350px —— 正是契约没建立造成的灾难。把契约写进 goal，是为了让这类问题在下一轮不再出现。

【关于「可定位」——这是全部验证的地基】
交付物必须支持**确定性寻址**：暂停动画后，能定位到任意时刻 t，并稳定渲染出该时刻的那一帧。
- 用 SMIL 时：`svg.pauseAnimations(); svg.setCurrentTime(t);`
- 用 CSS 时：容器加 `animation-play-state: paused`，用负的 `animation-delay` 定位到 t。
同一个 t，每次都必须渲染出逐像素完全相同的帧。做不到这一点，后面所有验证都无法复现——视为致命缺陷，优先修它。

**契约文件本身也必须可定位**：`animation_contract.json` 里的圆心/半径/相位必须与 `setCurrentTime(t)` 取出的第 t 帧物理位置一致（不是设计稿对齐，是运行时对齐）。验证者可以拿契约里的圆心去量每一帧的实际位置，误差超过 3px 就算契约失效。

【何为「完美」——需对照以下**全部**标准评判】
1. **像鹈鹕**：长喙下方巨大的喉囊是决定性特征，缺了就只是一只普通鸟；另有蹼足、丰满的身体、白 / 灰色羽毛。
2. **在骑车**：翅膀搭在车把上，身体跨在车座上，脚踩在踏板上，车架几何真的能承载一只鸟——而不是一只鸟浮在自行车旁边。
3. **自行车是自行车**：两个辐条车轮、车架三角、链条、车把、车座，且在**每一帧**都成立，不能只在某一帧对。
4. **运动是真的、且自洽**：
   - 车轮转动方向与前进方向一致，转速与前进速度匹配（不打滑、不空转、不倒转）；
   - 脚在整个踩踏周期内贴合踏板，不滑步、不脱离、不穿过曲柄；
   - 腿有真实的往复，膝与踝的朝向合理；
   - 身体随踏频有微幅起伏，头部保持相对稳定；
   - 地面 / 背景有与前进速度一致的视差位移。
5. **时间上是连贯的**：没有逐帧跳变、没有部件闪烁、没有穿模；曲线运动要是曲线，不是折线。
6. **循环无缝**：t=0 的帧与 t=T 的帧逐像素一致；接缝处不得有可见的「跳一下」。
7. **工艺**：构图干净、配色刻意、Z 轴层次正确、有地面线或场景暗示。风格由你定——扁平、几何、编辑风都可以——但要统一且坚定。

【方法提示】
- **先搭结构再动**：手工写 SVG 并审查分组结构。按 `<g id="wheel-front">`、`<g id="frame">`、`<g id="bird">`、`<g id="leg-near">` 这样拆开，**把要动的东西各自放进能独立 transform 的组里**。结构分对了，之后每个缺陷都是一次局部改动；分错了，每次修补都等于在整张图上重刷。**所有具体坐标和半径必须来自契约文件，不要现场手算手画**——契约一旦冻结，SVG 就只是"把契约里的数字渲染出来"。
- **用采样「看」动画**：写一个采样脚本（playwright 已装，用 chromium 截图 SVG），对 t = 0, T/24, 2T/24 … 逐帧截图，拼成一张 contact sheet——一次就能看完整条时间线。这比盯着一帧看有效得多。
- **用几何测量代替肉眼**（python3 + PIL 读像素）：
  - 在前后轮上各放一个高对比度标记点，追踪它随 t 的角度：应单调变化，且与前进方向一致；
  - **脚-踏板圆心距离随 t 的序列**：合格应 `max(dist) ≤ 3px`；出现阶跃就是滑步；
  - **契约校验**：读契约文件里的曲柄圆心 / 髋圆心 / 半径 / 腿长，跟每一帧 SVG 里对应 `<g>` 或 `<circle>` 元素的实测坐标逐一对比，误差应 ≤ 3px；
  - 追踪喉囊区域，确认整个周期内它始终位于头部下方、形状稳定；
  - 逐像素比较相邻帧的差异幅度，突变尖峰就是跳帧的确切坐标；
  - 逐像素比较 t=0 与 t=T，差值必须为 0（无缝循环）。
- **短循环迭代**：采样 → 测量 → 发布**带时刻 t 和具体坐标**的缺陷清单 → 修。多次小步调整，胜过一次性英雄式重写。

【验证（Validation）——执行，并发布证据】
- **必须渲染并发布一张覆盖全时间轴的 contact sheet**：以 t = 0, T/24, 2T/24 … T 共 N+1 帧（N≥24，即每秒≥6 帧）拼成一行接触表。这是**强制证据项**，不是可选的方法提示——没有它，无法判断运动是否连贯，直接视为未验证。
- 另外单独发布至少 3 个关键时刻的单帧（必须包含 t=0 与 t=T，另选一个 t∈(0,T) 的中间帧）。
- 发布浏览器控制台输出，错误必须为空。
- 发布**测量数据**，不要只给形容词：
  - 轮子角度-时间表（轮上标记点的角度应单调变化且与前进方向一致）
  - **脚-踏板圆心距离随 t 的序列**：`max(dist(脚心, 踏板圆心)) ≤ 3px` 是硬红线；超出即打滑/脱脚，直接不合格
  - 首尾帧逐像素差异值：必须全为 0（无缝循环）
  - 相邻帧差异的峰值位置：反映跳帧点，应与视觉跳变位置一致
  - 契约文件各常量 → 实际帧位置的偏差：圆心的 x/y、半径、腿长 L 的实测误差都应 ≤ 3px
- 必须有一名**未参与该文件绘制**的成员独立复现：自己重新采样、另选时刻与角度，确认结论一致。自己验证自己不算验证。
- 必须做一次**对抗性验证**：专门回答「这段动画最先在哪个 t 露馅？」——去找滑步、穿模、跳帧，以及「该动却没动」的部件，给出时刻与坐标。即便没找到，也要说明你检查了哪些位置、用了什么判据。
- 汇报时给出：产物路径 + 实际执行的命令 + 实际输出数据。

【分工】切片不预先分配，由看板认领产生。本目标不规定谁负责哪一片、也不规定步骤顺序。

【收工】只有以下条件全部满足才算可以了：交付物已生成；独立验证已完成并给出不同时刻的复现结果；对抗性验证已给出结论。收工说明里写清：谁验证的、验证了什么、结论是什么。

## 粘贴区 ↑↑↑

---

## 推荐设置（不要粘贴）

| 项 | 值 | 理由 |
|---|---|---|
| 显示名称 | `PELICAN LOOP` | 不填的话集群名 = 正文大写前 80 字，id 会变成乱码 slug |
| 智能体数 | 6 | 需要覆盖：结构搭建 / 运动实现 / 逐帧测量 / 独立验证 / 对抗性验证，5 个是最低配，6 个留一个余量 |
| 预算 USD | 12 | 采样-测量-修复的短循环意味着大量多轮迭代，比单张图那次更烧 |

**为什么锁 SMIL / 声明式动画**：动画验证的全部难度在于「同一个时刻能否被复现两次」。SMIL 的 `pauseAnimations()` + `setCurrentTime(t)` 让任何队友在任意机器上都能取到逐像素相同的第 t 帧；JS/canvas 方案要额外约定一套 seek 接口才等价。把可寻址性写成硬约束，是让后面所有测量都成为硬证据的前提。

**为什么禁逐帧贴图**：那是绕开「动画」这道题的做法——它不产生运动模型，也就无从检验运动自洽性（轮转与前进匹配、脚不滑步），而这些正是这个任务唯一的价值所在。

**为什么加「协作契约」段**：上一轮同样的任务翻车在**运动学耦合没被协调**——腿和踏板分属两个切片，各自手写圆心和半径，脚每圈只碰到踏板 1 帧、其余时间 200–350px 之外。系统不会自动检查"这两个切片说的圆心是不是一回事"，只能靠 goal 里显式地规定"契约优先、后续只许引用"。这是把 swarm 的分工从「按部件切」升级成「按耦合切」的最小代价。

**为什么把 contact sheet 从可选升级为必做**：原版本 contact sheet 只出现在「方法提示」里，是"建议"；这次 bug 恰好证明——单帧截图 / XML 合法 / dur 同步 三道防线全绿也抓不到运动学错误，只有把整段时间线摊平（≥24 帧的 contact sheet + 逐帧几何断言）才有一线机会。既然必须做，就写进验证段的硬证据清单里，而不是留在方法提示里让 agent 有理由偷懒。

**注意**：目标里的 `final_output/pelican-anim.svg` 路径只是给队友复现用的坐标，**系统不会机器校验它**（设计文档提过的 `final_output_file` 硬校验在代码里没有实现）。所以「交付物存在且合格」必须由验证段兜住，不能指望系统拦。
