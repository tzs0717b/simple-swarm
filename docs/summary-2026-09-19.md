# 2026-09-19 工作总结：让集群的协作「看得见、留得住、交得出」

## 0. 起点（你的口径，逐字留在案）

- 「这也太垃圾了…前段显示不完全…那肯定要让他们增加交流啊。而且你看最后一个 agent 还在干活的时候 swarm 就已经 done 了。反正你要首先提高集群的沟通能力，智力，然后再提升速度。」
- 「我建议，还是就是要鼓励 agent 之间的交流。」
- 去中心化：「经理 agent 还是算了，保持去中心化，我们系统在有人尝试 done 时打回，并且注入在邮箱广播问问其他人的指令。」
- 协议：≤10 agent / 15–45 分钟一轮 / 闭卷（**规格文件不进工作区，原始题面 TASK.md 可以进**）。

## 1. 机制：今天改了 7 件事（按提交顺序）

| 提交 | 时间 | 改了什么 | 一句话 |
|---|---|---|---|
| 33c99b0 / c432922 | 18:21 / 18:32 | 前端重写 + 集群标题上色 | 总览页、集群内子导航、看板支持多人同干一片 |
| 576371d | 19:00 | 修续跑路径 | 续跑必须广播新指令；实测续跑静默会让 4 个 agent 抱着上一轮旧邮件原地打转一整轮 |
| 5a828bd | 20:21 | **P1 工作区 git 留档** | 每步由系统自动提交并**署名**；改动清单来自工作区 diff → bash heredoc 也归因（B10 正解）；.git 被删能从工作区外的影子仓库恢复 |
| 8ad08dc | 21:06 | **P2 文件版本会说话** | 同一文件换手时系统**点对点**播报给上一版作者（你那版在哪个 sha、git show 怎么取回）；现场提示新增「文件版本」段；无效的「文件独占」换成 git 三条规矩 |
| ba9bae6 | 21:06 | 清理 | 自检产生的 gitrepos/gate-*.git 误入源码库（372 文件），加 .gitignore 并移除 |
| abb267f | 21:19 | **P3 收口兜底** | 墙钟最后 10% 把「已认领但没人交付」的片按现状入账（系统身份、故意不过验收闸、证据带文件版本+验收结果），并广播全队 |
| b79a4a9 | 21:54 | **P4 + P5 + P6** | 保底切法不再离题（从题面抠交付件名）；让 agent 自己交付（有产出的现场提示 + 验收跑绿时系统点名）；空转/哑火干预（零贡献者点名 + 连续 2 次不调工具点名） |
| 8e9e87f | 22:31 | **P7-a + P7-b** | 协商闸过半降级（一个沉默的 agent 不再锁死全队的 5 分钟）；墙钟到点打**最终快照**并广播最终版本号（交付后改动过就以最终版本为准） |

## 2. 真跑数据（今天跑的 5 轮，全部 0 失败）

| 集群 | 调用 | 花费 | 邮件 | 交付(agent 主动) | 产物 | 板子 |
|---|---|---|---|---|---|---|
| p2482-diandian3（dots 钉死）| 102 | $2.6973 | 17 | 0 | 输出错 | — |
| p2482-auto（auto 异构）| 144 | $3.7350 | 23 | 0 | 能编译但挂死 | — |
| p2482-p3 | 76 | $2.1873 | 24 | 3（**0**）| 编译不过 | 离题（画图题保底）|
| p2482-p4 | 109 | $3.5064 | 31 | 6（**4**）| 编译不过 | ✅ 贴题（带 p2482.cpp）|
| p2482-p7 | 21 | $0.3036 | 9 | —（被你叫停）| — | — |

**交付率这条主线：0 → 0 → 3（全靠系统兜底）→ 6（其中 4 片由 agent 自己交）**。

## 3. 今天挖到的真问题（比代码本身值钱）

1. **交付率 0 是结构性的**：产物能编译、agent 自己写的测试也真跑绿了，但 76 次调用里 complete_slice 被调用 **0 次** → 不是「交不出来」，是没人做「交付」这个动作。
2. **B10：agent 多数用 bash heredoc（cat > p2482.cpp << 'EOF'）写文件**，绕过 write 工具 → 提示词层的「文件独占」拦不住、账本里的写入事件也不准。git 留档把归因换到「工作区 diff」上才解决。
3. **板子完全不贴题**：保底切法硬编码的是动画实验的片（「主体静态造型」「相邻帧像素差」），给信息学题架出 6 片画图片的活。
4. **一个沉默的 agent 锁死全队**：协商闸要求全员广播过才放行，两轮都有人从头到尾不说话 → 板一直空 → 只能靠保底救场。
5. **交付后产物还在变**：交付发生在 90%，agent 之后继续改主产物（最后一次改动在墙钟**结束之后**）→「验过的版本」和「最终版本」不是一个东西，于是证据看着是真的、产物却是坏的。
6. 一堆小 bug：runner.ts 里漏进 TS 的 chr(10)（Python 写法，一触发就 ReferenceError，账本核实**从未触发**）；resumeAnnounced 用了没声明；我自己误提交 372 个自检产物、误报「交付 0」（slice.completed 是顶层事件、mail.sent 的 swarmId 在嵌套里）；**同一个「模板→Python→写文件」转义坑犯了 3 次**（chr(10)、board.ts 的换行、NL 变量名被写进 TS）。

## 4. 今天花了多少

| 项 | 金额 |
|---|---|
| 5 轮真跑 | **$12.4296** |
| llm.ts 误删导入导致的重跑 | ~$1.87 |
| **合计** | **≈ $14.30** |

（token 合计约 6.9M。）

## 5. 自检规模（全部绿）

| 自检 | 断言数 |
|---|---|
| import-check（静态导入扫描）| 45 文件 / 0 缺失 |
| git-check（留档 + 版本/换手/交付时机/兜底证据）| **33 → 69** |
| board-check（协商立板 + 保底切法）| **25 → 40** |
| challenge-check（质疑机制）| 15 |
| gate-check（收工闸/孤儿回收/累计放弃）| 52 |
| oxlint | 0 errors |

## 6. 明天接着干的候选（按我判断的优先级）

1. **P7 的验证轮**：p2482-p7 是你叫停的那一轮，P7-a（板由 agent 自己架起来）与 P7-b（最终快照）都**还没看到真跑数据** —— 明天第一件事就是重跑一轮看这两条。
2. **产物质量**：三轮的 p2482.cpp 都编译不过。最后一分钟改坏主产物是直接原因，考虑 95% 时**冻结主产物**（只许小修）。
3. **前端「文件版本」页**：GET /api/swarms/:id/files 已经能查（写者/次数/最新 sha/提交历史），前端还没接。
4. **速度**（你最早三条诉求里的第三条）：今天完全没动 —— 均延迟 8–12 秒、单轮 15 分钟里有效步数不高，等协作稳定了再碰。

## 7. 操作备忘

- 服务：cd ~/code/swarm && ./start.sh restart|stop|status（**现在后端仍在跑，只是那轮已停**；要一起关就 ./start.sh stop）。
- 跑一轮：bash ~/.dsh/round.sh <名称> [人数] [预算]（自动模式、异构 lane）。
- 出报告：python3 ~/.dsh/p4b.py（读 ~/.dsh/round-last-id）。
- 看一轮的版本：git --git-dir=simple-swarm-server/gitrepos/<id>.git log --oneline。
- 仓库：github.com/tzs0717b/simple-swarm（今天的提交全部已推送，HEAD 8e9e87f）。
- 文档：docs/bugs-2026-09-19.md（bug 审计）、docs/plan-git-workspace.md（三步走与进度）。
- 注意：write 工具在这台机器上会 EACCES（临时目录硬链接被拒），写文档用 cat heredoc。

