# Bug 审计（2026-09-19，p2482-diandian3 尸检引出）

证据都来自账本实测，不是猜的。状态：✅已修 / 📋已定位待拍板。

## ✅ 本批已修

### B1 「只回话不调工具」原地重试白烧
- 现场：dots 模型 6 次 no-tool（hannah 3 / kevin 2 / alfred 1）。llm.ts 先催 1 次（同上下文），
  runner.ts 再整轮重试 2 次 → 一次 no-tool 最多烧 6 次调用；全轮才 52 次调用，≈1/3 浪费在这。
- 修：runner.ts 里 no-tool 不再原地重试（同上下文重试必然同病），计入大脑异常后直接进下一轮排程。

### B2 主交付文件被互相覆盖、无人感知
- 现场：p2482.cpp 被 3 个 agent 覆盖写 5 次（hannah×2 / alfred×2 / kevin×1）；
  名义 owner lucille（认领集成片）一次都没写；最终版是「认领测试片」的 alfred 写的。
- 修（提示词层）：开工广播加「文件独占」规则 —— 主交付文件广播点名唯一 owner；
  非 owner 想写替代实现必须用带自己名字的后缀（main.kevin.cpp），merge 由 owner 决定；
  测试/对拍脚本各自独立命名。（= 用户提的「后缀+merge」的简化版：后缀只用于替代实现，主文件单一 owner，避免全员后缀+大 merge 的复杂度）

## 📋 已定位、待拍板

### B3 交付率 0：complete_slice 全程 0 次尝试
- 现场：交作业闸①(50%)/②(80%)/最后点名催都发了，4 人无一调用 complete_slice；墙钟耗尽。
- 建议：auto-ship 兜底 —— 墙钟 ≥95% 时系统替未交付的已认领片自动收口：
  evidence=文件清单+大小+最后写入者+（若有）验收脚本输出，标记「系统自动交付（未经 agent 确认）」。
  宁交半成品也不交白卷；与「立板截止兜底」同一哲学。

### B4 认领不问责 / 干活不认领
- 现场：lucille 认领 2 片、26 步、0 次写文件；hannah 0 认领、31 步、干了最重的活（写主文件+调试）。
- 建议：situation() 给每个 agent 显示「你名下未交付的片 + 距你上次写文件已过 N 分钟」；
  N 超阈值 → 系统邮件点名（可配置释放认领）。

### B5 验收没牙：stdout 污染没人抓
- 现场：最终 p2482.cpp 把 [LOG] 调试行打进 stdout（洛谷必 WA）+ 最后一行答案错（J J J D ≠ J J J J J J D）；
  机械验收片（test_runner.py）到死没写完没跑过。
- 建议：complete_slice 的自动验收除 check*/verify*/test*.py 外，工作区有 *.in 样例时尝试编译+跑一遍，
  输出贴进回执；stdout 混入非答案行直接打回。

### B6 no-tool 判定靠 JSON.stringify(...).includes(字符串)
- runner.ts:773 —— 该短语出现在任何字段（如 observation 复述）都会误计大脑异常。建议 step 加显式 noTool:true 字段。

### B7 /api/swarms/:id 的 slices 字段永远 []
- 投影没接上；权威数据在 /api/swarms/:id/slices。前端一律走后者（本次 UI 重写已按此办）。

### B8 diandian 车道 21~40% 调用超时（非 swarm bug）
- 19/48（上轮）与 11/52（本轮）失败，多为 >30s 超时；keypool 里 4 个 key 全 active。
- 钉死模型时不触发故障转移（设计如此）→ 时间烧在重试。要稳：换车道或允许钉死后仍故障转移。
